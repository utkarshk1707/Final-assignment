CREATE TABLE IF NOT EXISTS userdata(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT,email TEXT,contact INTEGER,password TEXT);
INSERT or IGNORE INTO product(id, name,email,contact,) VALUES (1, 'Ionic Academy', 1);
